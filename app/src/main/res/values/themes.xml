<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main app theme for Compose -->
    <style name="Theme.ReelGuard" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Block activity theme with black background -->
    <style name="Theme.ReelGuard.Block" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
